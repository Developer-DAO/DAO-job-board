import styled from 'styled-components';
import {Title2} from '../../styles/styles';


export default function GigItem() {
  return (
        <GigTitle>This is a test</GigTitle>
  )
}

const GigTitle = styled(Title2)`
  font-size: 1.5rem;
`;
